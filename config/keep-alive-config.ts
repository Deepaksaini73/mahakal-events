export const KEEP_ALIVE_CONFIG = {
  // Add URLs of other Supabase projects that need to stay alive
  // Each should have their own /api/keep-alive endpoint
  otherProjects: [
    'https://gym-protein-tool.vercel.app/api/keep-alive',
  ],
}
